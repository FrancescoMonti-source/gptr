% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_local_backends.R, R/local_detect.R
\name{list_local_backends}
\alias{list_local_backends}
\title{List configured local backend endpoints}
\usage{
list_local_backends()

list_local_backends()
}
\value{
A named character vector of base URLs for supported local backends.

A data frame with backend, name, base_url and probe.
}
\description{
Returns the \emph{configured} local backend endpoints from the package defaults
(set in \code{.onLoad()} inside \code{zzz.R}) without attempting to detect whether these
backends are actually running or reachable.
}
\details{
To change these defaults globally, set them via \code{options()} in your \code{.Rprofile}
or session, for example:
\preformatted{
options(
  gpt.lmstudio_base_url = "http://localhost:1234/v1/chat/completions",
  gpt.ollama_base_url   = "http://localhost:11434/v1/chat/completions"
)
}
}
\examples{
list_local_backends()
}
