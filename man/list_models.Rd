% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_models.R
\name{list_models}
\alias{list_models}
\title{List models from OpenAI, LM Studio, Ollama, or LocalAI.}
\usage{
list_models(
  provider = c("any", "lmstudio", "ollama", "localai", "openai"),
  url = NULL,
  base_url = NULL,
  timeout = getOption("gpt.timeout", 5),
  openai_api_key = Sys.getenv("OPENAI_API_KEY", "")
)
}
\arguments{
\item{provider}{"any","lmstudio","ollama","localai","openai".}

\item{url}{Optional full models URL (e.g. "http://127.0.0.1:11434/v1/models").}

\item{base_url}{Optional chat-completions base URL; we derive \verb{/v1/models} etc.}

\item{timeout}{Request timeout, seconds.}

\item{openai_api_key}{API key for OpenAI (required when provider == "openai" or when url points to api.openai.com).}
}
\value{
A tibble with at least columns \code{backend} and \code{id}. Additional columns
are returned when the server exposes richer model metadata.
}
\description{
Use either a full \code{url} (to \verb{/v1/models}) or a \code{base_url} (we derive candidates),
or specify \code{provider = "lmstudio"|"ollama"|"localai"|"openai"}, or leave as
\code{provider = "auto"} to query whatever is available (OpenAI if key set, plus reachable locals).
}
