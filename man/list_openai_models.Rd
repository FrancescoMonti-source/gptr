% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_openai_models.R
\name{list_openai_models}
\alias{list_openai_models}
\title{List Available Models from the OpenAI API}
\usage{
list_openai_models(url = "https://api.openai.com/v1/models")
}
\arguments{
\item{url}{Character string giving the OpenAI models endpoint.
Defaults to \code{"https://api.openai.com/v1/models"}.}
}
\value{
A \link[tibble:tibble]{tibble} where each row represents a model and columns
typically include:
\describe{
\item{\code{id}}{The model identifier (e.g., \code{"gpt-4o"}, \code{"text-embedding-ada-002"}).}
\item{\code{object}}{The type of object, usually \code{"model"}.}
\item{\code{owned_by}}{The owner of the model (e.g., \code{"openai"}, \code{"system"}, \code{"user"}).}
\item{\code{created}}{The creation date of the model, converted to a \code{Date} object.}
Additional columns may be present depending on OpenAI's API version.
}
}
\description{
Retrieves and returns the list of models available to the authenticated user
from the \href{https://platform.openai.com/docs/api-reference/models/list}{OpenAI API}.
}
\details{
This function sends a GET request to the specified OpenAI API endpoint,
authenticating with the \code{OPENAI_API_KEY} environment variable.
The JSON response is parsed, and the \code{data} field is converted into a tidy tibble.

The \code{created} field is returned as a UNIX timestamp in seconds by the API
and is converted to a \code{Date} for convenience.

An error is raised if:
\itemize{
\item The \code{OPENAI_API_KEY} environment variable is missing or invalid.
\item The request fails (e.g., network issues, unauthorized access).
}
}
\examples{
\dontrun{
# Make sure your API key is set in the environment:
Sys.setenv(OPENAI_API_KEY = "sk-...")

# List available models
list_openai_models()

# List models from a custom endpoint (rare)
list_openai_models("https://api.openai.com/v1/models")
}

}
\seealso{
\itemize{
\item \code{\link[httr:GET]{httr::GET()}} for making HTTP requests.
\item \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}} for parsing JSON.
\item OpenAI Models API: https://platform.openai.com/docs/api-reference/models
}
}
