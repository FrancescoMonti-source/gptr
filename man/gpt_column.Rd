% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpt_column.R
\name{gpt_column}
\alias{gpt_column}
\title{Extract Structured Data from Free Text via LLM Completion (Orchestrator)}
\usage{
gpt_column(
  data,
  col,
  prompt = NULL,
  keys = NULL,
  instruction = NULL,
  template = NULL,
  provider = c("auto", "local", "openai", "lmstudio", "ollama", "localai"),
  backend = NULL,
  base_url = NULL,
  model = NULL,
  temperature = 0.2,
  file_path = NULL,
  image_path = NULL,
  .coerce_types = TRUE,
  coerce_when = NULL,
  infer_types = FALSE,
  na_values = c("NA", "N/A", "null", "None", ""),
  auto_correct_keys = getOption("gptr.auto_correct_keys", TRUE),
  keep_unexpected_keys = getOption("gptr.keep_unexpected_keys", FALSE),
  fuzzy_model = getOption("gptr.fuzzy_model", "lev_ratio"),
  fuzzy_threshold = getOption("gptr.fuzzy_threshold", 0.25),
  structured = c("auto", "backend_schema", "prompt_schema"),
  relaxed = FALSE,
  return_debug = TRUE,
  verbose = FALSE,
  progress = TRUE,
  multi_value = c("first", "error", "list"),
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing the text column.}

\item{col}{Unquoted name of the text column to send to the LLM.}

\item{prompt}{Legacy/raw prompt interface. Supply either a character template
with \code{{text}} / \code{{json_format}} placeholders or a function \verb{(text, keys) -> string}. This path is kept for expert use and backward compatibility.}

\item{keys}{Optional named list defining expected JSON keys and their type or allowed set.}

\item{instruction}{Preferred prompt interface. A plain-language description of
what to extract. \code{gptr} uses it to build the final prompt scaffold for the
resolved schema mode.}

\item{template}{Optional advanced prompt template used together with
\code{instruction}. Supports \code{{instruction}} and \code{{text}}. If \code{{text}} is
omitted, \code{gptr} appends a standard \verb{Text:} section automatically.}

\item{provider}{One of "auto", "local", "openai", "lmstudio", "ollama", "localai".
With \code{provider = "auto"}, \code{gpt_column()} follows the same configured route
selection as \code{gpt()}: \code{base_url}, \code{backend}, \code{gptr.local_base_url}, then
\code{gptr.local_prefer}, with OpenAI fallback only when no local route is
configured and remote use is allowed.}

\item{backend}{Optional backend to use when \code{provider = "local"}.}

\item{base_url}{(now accepted by gpt_column() too): “Optional. Pin a specific local endpoint (…/v1 or …/v1/chat/completions).”}

\item{model}{Model identifier to request from the provider/backend. When
\code{NULL}, the chosen route uses its default model.}

\item{temperature}{Sampling temperature for the model.}

\item{file_path, image_path}{Optional file or image paths attached to each model call.}

\item{.coerce_types}{Row-level coercion toggle (default TRUE). Explicit
logical coercion accepts \code{true/false}, \code{yes/no}, \code{oui/non}, and \code{1/0}.}

\item{coerce_when}{Optional named list of per-key target types used for row-level coercion.}

\item{infer_types}{Logical; when no schema is provided, infer column types (default FALSE).
If a schema (\code{keys}) is provided, it is always used for final typing.}

\item{na_values}{Values treated as NA at multiple stages.}

\item{auto_correct_keys}{Logical; fuzzy-correct unexpected key names (unique match only).
Fuzzy key correction is handled by json_keys_align(); control with \code{fuzzy_model}
("lev_ratio" or "lev") and \code{fuzzy_threshold}.}

\item{keep_unexpected_keys}{Keep keys not listed in \code{keys}.}

\item{structured}{Extraction strategy. \code{"auto"} (default) uses backend-enforced
schemas for OpenAI and otherwise falls back to prompt-managed schemas unless
the chosen local backend was explicitly opted in via
\code{options(gptr.backend_schema_backends = ...)}. \code{"backend_schema"}
requires backend schema support on the chosen route. \code{"prompt_schema"}
always uses the prompt-managed path. Legacy aliases \code{"native"} and
\code{"repair"} are still accepted. The managed \code{instruction} / \code{template}
prompt scaffold follows this resolved mode automatically.}

\item{relaxed}{If TRUE and \code{keys} is NULL, allow non-JSON / raw outputs.}

\item{return_debug}{If TRUE, add \code{.raw_output}, \code{.schema_mode},
\code{.invalid_rows}, and \code{.invalid_detail}. Default TRUE.}

\item{verbose}{Print repair/validation messages.}

\item{progress}{Logical. Progress Bar, defaults to TRUE.}

\item{multi_value}{Strategy when a key maps to multiple values. Passed to
\code{row_to_tibble()}. Defaults to \code{"first"}.}

\item{...}{Extra args passed to \code{gpt()} (e.g., \code{response_format}).}
}
\description{
Sends each row of a text column to an LLM with an instruction-first or legacy
prompt interface, repairs/parses JSON, validates and aligns it to a schema,
then returns the structured columns bound to the input.
}
